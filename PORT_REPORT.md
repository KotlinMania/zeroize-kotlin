# Port report — `zeroize-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `zeroize`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 34 `use` sites from 11 other repos
- **Outbound**: 0 `use` sites to 0 other repos
- **Unresolved external**: 1 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `zeroize` | `zeroize` | 34 | 0 | COD-196 | 15 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (34 edges, 11 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `rustls-kotlin` | `rustls` | 9 |  |
| `age-kotlin` | `age` | 6 |  |
| `aws-config-kotlin` | `aws-config` | 4 |  |
| `ed25519-dalek-kotlin` | `ed25519-dalek` | 4 |  |
| `tiny-http-kotlin` | `tiny_http` | 3 |  |
| `crypto-box-kotlin` | `crypto_box` | 2 |  |
| `zip-kotlin` | `zip` | 2 |  |
| `aws-credential-types-kotlin` | `aws-credential-types` | 1 |  |
| `aws-sigv4-kotlin` | `aws-sigv4` | 1 |  |
| `codex-kotlin` | `codex-responses-api-proxy` | 1 | ✅ |
| `keyring-kotlin` | `keyring` | 1 |  |

### Symbol-level demand (3 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `zeroize::Zeroize` | 19 | 1 | 18 | 7 |
| `zeroize::Zeroizing` | 13 | 0 | 13 | 8 |
| `zeroize::ZeroizeOnDrop` | 2 | 0 | 2 | 1 |

#### Per-symbol detail (top 3 symbols)

##### `zeroize::Zeroize` (19 sites, 1 codex, 18 other, 7 caller crates)

Codex call sites:

- `codex-kotlin/codex-responses-api-proxy` `src/read_api_key.rs:4` — `use zeroize::Zeroize`

Other workspace call sites:

- `age-kotlin/age` `src/cli_common/file_io.rs:12` — `use zeroize::Zeroize`
- `age-kotlin/age` `src/primitives/stream.rs:11` — `use zeroize::Zeroize`
- `age-kotlin/age` `src/scrypt.rs:18` — `use zeroize::Zeroize`
- `age-kotlin/age` `src/ssh/identity.rs:21` — `use zeroize::Zeroize`
- `age-kotlin/age` `src/x25519.rs:16` — `use zeroize::Zeroize`
- `crypto-box-kotlin/crypto_box` `src/secret_key.rs:11` — `use zeroize::Zeroize`
- `ed25519-dalek-kotlin/ed25519-dalek` `src/hazmat.rs:24` — `use zeroize::Zeroize`
- `ed25519-dalek-kotlin/ed25519-dalek` `src/signing.rs:40` — `use zeroize::Zeroize`
- `keyring-kotlin/keyring` `src/windows.rs:19` — `use zeroize::Zeroize`
- `rustls-kotlin/rustls` `src/crypto/aws_lc_rs/hpke.rs:12` — `use zeroize::Zeroize`
- `rustls-kotlin/rustls` `src/crypto/cipher.rs:4` — `use zeroize::Zeroize`

_… 7 more call sites._

##### `zeroize::Zeroizing` (13 sites, 0 codex, 13 other, 8 caller crates)

Other workspace call sites:

- `age-kotlin/age` `src/primitives/armor.rs:9` — `use zeroize::Zeroizing`
- `aws-config-kotlin/aws-config` `src/login/cache.rs:19` — `use zeroize::Zeroizing`
- `aws-config-kotlin/aws-config` `src/login/token.rs:13` — `use zeroize::Zeroizing`
- `aws-config-kotlin/aws-config` `src/sso/cache.rs:17` — `use zeroize::Zeroizing`
- `aws-config-kotlin/aws-config` `src/sso/token.rs:34` — `use zeroize::Zeroizing`
- `aws-credential-types-kotlin/aws-credential-types` `src/credentials_impl.rs:12` — `use zeroize::Zeroizing`
- `aws-sigv4-kotlin/aws-sigv4` `src/sign/v4a.rs:11` — `use zeroize::Zeroizing`
- `crypto-box-kotlin/crypto_box` `src/lib.rs:187` — `use zeroize::Zeroizing`
- `rustls-kotlin/rustls` `src/msgs/persist.rs:5` — `use zeroize::Zeroizing`
- `tiny-http-kotlin/tiny_http` `src/lib.rs:95` — `use zeroize::Zeroizing`
- `tiny-http-kotlin/tiny_http` `src/ssl/openssl.rs:7` — `use zeroize::Zeroizing`
- `tiny-http-kotlin/tiny_http` `src/ssl/rustls.rs:7` — `use zeroize::Zeroizing`

_… 1 more call sites._

##### `zeroize::ZeroizeOnDrop` (2 sites, 0 codex, 2 other, 1 caller crates)

Other workspace call sites:

- `ed25519-dalek-kotlin/ed25519-dalek` `src/hazmat.rs:24` — `use zeroize::ZeroizeOnDrop`
- `ed25519-dalek-kotlin/ed25519-dalek` `src/signing.rs:40` — `use zeroize::ZeroizeOnDrop`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

_No outbound edges to other workspace repos._

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

| package | edges | distinct symbols | importing members in this repo |
|---|---:|---:|---|
| `zeroize_derive` | 2 | 2 | `zeroize` |
